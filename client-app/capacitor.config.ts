import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'dev.kungraseri.gmt',
  appName: "Gardener's Multitool",
  webDir: 'dist',
  bundledWebRuntime: false,

};

export default config;

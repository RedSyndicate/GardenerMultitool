import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
	appId: 'org.redsyndicate.gm',
	appName: 'gardeners-multitool',
	webDir: 'dist',
	bundledWebRuntime: false
};

export default config;
